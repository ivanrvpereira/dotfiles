# Global mise configuration
# Manages language runtimes and Python CLI tools across machines
# Install all: mise install

[tools]
# ─── Language Runtimes ────────────────────────────────────
node = "latest"
python = ["3.13", "3.12", "3.11", "3.10"]
go = "latest"
rust = "latest"
java = "latest"
lua = "5.4"  # not "latest": Lua 5.5.0 (Dec 2025) + mise build layout breaks luarocks ("interpreter not found")
uv = "latest"

# ─── Dev Tools ────────────────────────────────────────────
# Note: OS packages manage jq/just/pre-commit; macOS uses Brewfile,
# Linux uses Aptfile plus bin/install-linux-mise-tools for fast-moving CLIs.
bun = "latest"
pnpm = "latest"
"npm:@llamaindex/liteparse" = "latest"
ruff = "latest"
black = "latest"

# ─── Python CLI Tools (pipx backend, uses uv) ────────────
"pipx:git-filter-repo" = "latest"
"pipx:marker-pdf" = "latest"
"pipx:poetry" = "latest"
"pipx:crawl4ai" = "latest"
"pipx:shell-gpt" = "latest"
"pipx:youtube-transcript-api" = "latest"

# ─── Language Servers ────────────────────────────────────
"npm:basedpyright" = "latest"
"npm:typescript-language-server" = "latest"
"npm:typescript" = "latest"
"go:golang.org/x/tools/gopls" = "latest"
rust-analyzer = "latest"

[settings]
# Use latest installed version when no version is specified
legacy_version_file = true
