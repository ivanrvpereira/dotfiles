# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
after-login-command = []
# Available commands: https://nikitabobko.github.io/AeroSpace/commands

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = [
    # JankyBorders has a built-in detection of already running process,
    # so it won't be run twice on AeroSpace restart
    'exec-and-forget borders active_color=0xff1688f0 inactive_color=0xff494d64 width=6.0'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 10

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = 'move-mouse window-lazy-center'

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodness#disable-hide-app
automatically-unhide-macos-hidden-apps = true




# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
preset = 'qwerty'


[gaps]
inner.horizontal = 5
inner.vertical = 5
outer.left = 5
outer.top = 5
outer.bottom = 5
outer.right = 5

[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    # All possible modifiers: cmd, alt, ctrl, shift

    # All possible commands: https://nikitabobko.github.io/AeroSpace/commands

    # See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
    # You can uncomment the following lines to open up terminal with alt + enter shortcut
    # (like in i3)
    # alt-enter = '''exec-and-forget osascript -e '
    # tell application "Terminal"
    #     do script
    #     activate
    # end tell'
    # '''

    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    alt-m = 'layout tiles horizontal vertical'
    alt-n = 'layout accordion'
    alt-f = 'layout floating tiling' # 'floating toggle' in i3
    alt-c = 'exec-and-forget $HOME/.config/aerospace/center-window.sh' # Center focused floating window


    # alt-enter = 'exec-and-forget open -n /Applications/Alacritty.app' # use Super + N to open new alacritty terminal
#cmd-enter = 'macos-native-fullscreen'
# cmd-q = 'close'
    cmd-e = 'macos-native-minimize'
    cmd-m = 'fullscreen'
    alt-left = 'workspace --wrap-around prev'
    alt-right = 'workspace --wrap-around next'
    alt-up = 'focus-monitor prev'
    alt-down = 'focus-monitor next'
#    cmd-shift-left = 'move-workspace-to-monitor prev'
#cmd-shift-right = 'move-workspace-to-monitor next'
# cmd-shift-c = 'reload-config'
    f11 = 'fullscreen'

    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
# cmd-shift-m = 'mode manage'
    cmd-shift-r = 'mode resize'

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    alt-h = 'focus left'
    alt-j = 'focus down'
    alt-k = 'focus up'
    alt-l = 'focus right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move
    alt-shift-h = 'move left'
    alt-shift-j = 'move down'
    alt-shift-k = 'move up'
    alt-shift-l = 'move right'


    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    cmd-1 = 'workspace TERM'
    cmd-2 = 'workspace CODE'
    cmd-3 = 'workspace BROWSE'
    cmd-4 = 'workspace MAIN'
    cmd-5 = 'workspace CHAT'
    cmd-6 = 'workspace PLAN'
    cmd-7 = 'workspace MEETING'
    cmd-8 = 'workspace NOTES'
    cmd-9 = 'workspace BG'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
    cmd-alt-1 = ['move-node-to-workspace TERM', 'workspace TERM']
    cmd-alt-2 = ['move-node-to-workspace CODE', 'workspace CODE']
    cmd-alt-3 = ['move-node-to-workspace BROWSE', 'workspace BROWSE']
    cmd-alt-4 = ['move-node-to-workspace MAIN', 'workspace MAIN']
    cmd-alt-5 = ['move-node-to-workspace CHAT', 'workspace CHAT']
    cmd-alt-6 = ['move-node-to-workspace PLAN', 'workspace PLAN']
    cmd-alt-7 = ['move-node-to-workspace MEETING', 'workspace MEETING']
    cmd-alt-8 = ['move-node-to-workspace NOTES', 'workspace NOTES']
    cmd-alt-9 = ['move-node-to-workspace BG']  # Don't follow - background workspace

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
    alt-tab = 'workspace-back-and-forth'
    # See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
    alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

    # Move workspace to specific monitor (by name pattern)
    alt-shift-1 = 'move-workspace-to-monitor built-in'
    alt-shift-2 = 'move-workspace-to-monitor ultrawide'
    alt-shift-3 = 'move-workspace-to-monitor arzopa'
    alt-shift-4 = 'move-workspace-to-monitor sidecar'

    # Move individual window to specific monitor
    ctrl-alt-1 = 'move-node-to-monitor built-in'
    ctrl-alt-2 = 'move-node-to-monitor ultrawide'
    ctrl-alt-3 = 'move-node-to-monitor arzopa'
    ctrl-alt-4 = 'move-node-to-monitor sidecar'

    # Focus specific monitor directly (ctrl-1/2/3/4)
    ctrl-1 = 'focus-monitor built-in'
    ctrl-2 = 'focus-monitor ultrawide'
    ctrl-3 = 'focus-monitor arzopa'
    ctrl-4 = 'focus-monitor sidecar'

    #
    cmd-h = [] # Disable "hide application"
    cmd-alt-h = [] # Disable "hide others"
    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
  #salt-shift-semicolon = 'mode service'
  # See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
#
    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    cmd-shift-comma = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
    b = ['balance-sizes', 'mode main'] # Equalize window sizes
    backspace = ['close-all-windows-but-current', 'mode main']

    # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
    #s = ['layout sticky tiling', 'mode main']

    alt-shift-h = ['join-with left', 'mode main']
    alt-shift-j = ['join-with down', 'mode main']
    alt-shift-k = ['join-with up', 'mode main']
    alt-shift-l = ['join-with right', 'mode main']

    down = 'volume down'
    up = 'volume up'
    shift-down = ['volume set 0', 'mode main']

[mode.resize.binding]
h = 'resize width -75'
j = 'resize height +75'
k = 'resize height -75'
l = 'resize width +75'
enter = 'mode main'
esc = 'mode main'




# Floating apps
[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.Preview'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.QuickTimePlayerX'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.colliderli.iina'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.linguee.DeepLCopyTranslator'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.openai.chat'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.ActivityMonitor'
run = 'layout floating'

# Workspace assignments
[[on-window-detected]]
if.app-id = 'com.anthropic.claudefordesktop'
run = 'move-node-to-workspace MAIN'

[[on-window-detected]]
if.app-id = 'notion.id'
run = 'move-node-to-workspace NOTES'

[[on-window-detected]]
if.app-id = 'com.apple.Notes'
run = 'move-node-to-workspace NOTES'

[[on-window-detected]]
if.app-id = 'md.obsidian'
run = 'move-node-to-workspace NOTES'

[[on-window-detected]]
if.app-id = 'net.shinyfrog.bear'
run = 'move-node-to-workspace NOTES'

[[on-window-detected]]
if.app-id = 'company.thebrowser.Browser'
run = 'move-node-to-workspace BROWSE'

[[on-window-detected]]
if.app-id = 'com.todesktop.230313mzl4w4u92'
run = 'move-node-to-workspace CODE'

[[on-window-detected]]
if.app-id = 'com.jetbrains.pycharm'
run = 'move-node-to-workspace CODE'

#[[on-window-detected]]
#if.app-id = 'com.github.wez.wezterm'
#run = 'move-node-to-workspace TERM'

#[[on-window-detected]]
#if.app-id = 'com.mitchellh.ghostty'
#run = 'move-node-to-workspace TERM'

[[on-window-detected]]
if.app-id = 'com.microsoft.Outlook'
run = 'move-node-to-workspace PLAN'

[[on-window-detected]]
if.app-id = 'com.todoist.mac.Todoist'
run = 'move-node-to-workspace PLAN'

[[on-window-detected]]
if.app-id = 'com.todesktop.210203cqcj00tw1'
run = 'move-node-to-workspace PLAN'

[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
run = 'move-node-to-workspace CHAT'

[[on-window-detected]]
if.app-id = 'com.microsoft.teams2'
run = 'move-node-to-workspace MEETING'
